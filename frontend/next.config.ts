import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /** Allow local FastAPI during dev if you later proxy; not required for client-side fetch to 127.0.0.1 */
};

export default nextConfig;
