import { MentalCoachChat } from "@/components/MentalCoachChat";

/**
 * Home route: client chat shell lives in `MentalCoachChat` for interactivity and fetch.
 */
export default function Home() {
  return <MentalCoachChat />;
}
