services:
  - type: web
    name: ember-mental-coach-api
    runtime: python
    buildCommand: pip install uv && uv sync
    startCommand: uv run uvicorn api.index:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false